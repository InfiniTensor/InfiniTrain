# Problems Log

| Date | Problem Name | Description | Repro Steps | Expected Result | Actual Result | Solution | Solved? |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-03-11 | LLaMA-3 FlashAttention 精度严重异常 | LLaMA-3 (1B) 基准测试中，Baseline Loss ~3.5，但 FlashAttention Loss ~14.6。 | 运行 `python3 benchmark.py` 的 LLaMA-3 1024 任务 | Loss ~3.5 (Baseline) | Loss ~14.6 (Worse than random init) | 根因：1. FlashAttention Kernel 假设输入布局为 `(B, H, T, D)`，但 `ScaledDotProductAttention` 传入 `(B, T, H, D)`，且 Kernel 中维度提取 `dims[1/2]` 错误映射 `H/T`。2. `gemm_ab_accum` 调用时参数 `N` 与 `K` 错位（传入 ValidRows/Cols 导致维度混淆）。修复：在 `ScaledDotProductAttention` 中显式转置输入为 `(B, H, T, D)`；修正 Kernel 维度提取与 GEMM 调用参数。验证：Loss 恢复至 ~3.51 (Baseline 3.51)。 | Yes |
| 2026-03-11 | benchmark 控制台看似卡住 | `benchmark.py` 使用 `subprocess.run(capture_output=True)`，子进程日志在结束前不会实时打印，长耗时 case（Flash-2048）期间终端无新输出，造成“卡住”错觉。 | 运行 `python3 benchmark.py` 到 `Flash-2048` 阶段 | 持续看到进度日志 | 终端长时间无输出，误判为卡死 | 改为 `subprocess.Popen(..., stdout=PIPE, stderr=STDOUT)` 流式读取并实时打印每步指标；已验证可持续输出 step/loss/tps/mem 且任务正常完成。 | Yes |
| 2026-03-11 | Benchmark 第 10 步 OOM | OOM 并非由训练主路径持续增涨导致，而是默认 `freq_generate_txt=10` 在第 10 步触发 `tokenizer->GenerateText(...)` 产生额外显存占用，叠加训练图后触发 `CudaGuardImpl::MallocAsync` 失败。 | 运行 `./build/gpt2 ... -num_iteration=20 -batch_size=2 -sequence_length=1024`（不显式设置 `-freq_generate_txt`） | 训练稳定跑完 | 在 step 10 后进入文本生成阶段并 OOM，堆栈指向 `cuda_guard_impl.cc:224` | 压测/基准命令增加 `-freq_generate_txt=9999`（禁用中途文本生成）；并保留较安全 batch（1024 用 BS=2，2048 用 BS=1）。已验证 Baseline/Flash 均可跑过 step 10 且正常结束。 | Yes |
| 2026-03-10 | Functional 接口编译错误 | 在 functional.h 中使用了 std::optional 但未包含头文件 | 运行 `make -j infini_train` | 编译通过 | 报错 `error: ‘std::optional’ has not been declared` | 在 functional.h 中添加 `#include <optional>` | Yes |
| 2026-03-10 | gpt2 链接失败 | 增加 Autograd 新类后静态库链接未更新到最新产物 | 运行 `make -j gpt2` | 生成 gpt2 可执行文件 | 报错 `undefined reference to vtable for infini_train::autograd::ScaledDotProductAttention` | 清理构建目录并重新执行 cmake 与 make 全量构建 | Yes |
| 2026-03-10 | flash_attention.cu 编译失败 | CUDA 实现误用了不存在的 Tensor API 名称 | 运行 `make -j gpt2` | 编译通过 | 报错 `Tensor has no member GetDType/data/mutable_data` | 改为项目实际 API：`Dtype()` 与 `DataPtr()` | Yes |
| 2026-03-10 | 训练中途 OOM | 在进行 FlashAttention 集成测试时，Baseline 和 Flash 版本均在第 10 步后崩溃 | 运行 `python3 benchmark.py` (BS=4, Seq=1024) | 完成 20 步训练 | Step 10 后报错 `CUDA Error: out of memory`。Peak Memory: Baseline ~14GB, Flash ~11.5GB。显存（16GB）不足或碎片化导致。 | 初步缓解为降低 Batch Size（1024: 4->2; 2048: 2->1）；最终根因与彻底修复见 2026-03-11 条目（禁用中途文本生成）。 | Yes |
| 2026-03-10 | Autograd 输入梯度检查失败 | FlashAttention Backward 返回梯度数量与输入数量不一致（Mask 可选导致） | 运行 `gpt2 --flash=true` | 正常反向传播 | 报错 `Check failed: grad_inputs.size() == next_functions_.size() (4 vs. 3)` | 修复 `ScaledDotProductAttention::Backward` 中的逻辑，根据输入实际数量动态决定是否返回 Mask 梯度 | Yes |
