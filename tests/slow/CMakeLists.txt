# ============================================================================
# Slow tests
# ============================================================================
# 重构版本：使用 infini_train_add_test 宏简化配置
#
# 新增测试只需 1 行：
#   infini_train_add_test(test_name SOURCES test_name.cc LABELS slow cpu)
# ============================================================================

# -----------------------------------------------------------------------------
# Slow tests (标签包含 slow)
# -----------------------------------------------------------------------------
infini_train_add_test(test_slow SOURCES test_slow.cc LABELS slow cpu cuda distributed)
